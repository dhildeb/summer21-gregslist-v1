export class Job {
  constructor(
    jobPosition, location, company, wage, fullTime, description
  ) {

    this.jobPosition = jobPosition
    this.location = location
    this.company = company
    this.wage = wage
    this.fullTime = fullTime
    this.description = description
  }
}